type OAuthURL {
  url: String!
  provider: String!
}

extend type Query {
  me: User
  initiateOAuth(provider: String!): OAuthURL!
  linkedProviders: [String!]!
}

extend type Mutation {
  logout: Boolean!
}
